name: Publish Python Package

on:
  workflow_run:
    workflows: ["Build Python Package"]
    types:
      - completed

env:
  CLOUDSMITH_REPOSITORY: "staging"

permissions:
  contents: read
  actions: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Sanitize environment variables
        run: |
          echo "CLOUDSMITH_NAMESPACE=$(echo '${{ vars.CLOUDSMITH_NAMESPACE }}' | tr -d '[:space:]')" >> $GITHUB_ENV
          echo "CLOUDSMITH_SERVICE_SLUG=$(echo '${{ vars.CLOUDSMITH_SERVICE_SLUG }}' | tr -d '[:space:]')" >> $GITHUB_ENV
        shell: bash

      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: python-package
          path: dist
          repository: ${{ github.repository }}
          github-token: ${{ github.token }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: List directory contents
        run: ls -la dist/
        shell: bash

      - name: Install Cloudsmith CLI (OIDC)
        uses: cloudsmith-io/cloudsmith-cli-action@v1.0.1
        with:
          oidc-namespace: ${{ env.CLOUDSMITH_NAMESPACE }}
          oidc-service-slug: ${{ env.CLOUDSMITH_SERVICE_SLUG }}

      - name: Verify Cloudsmith authentication
        run: |
          echo "Testing Cloudsmith authentication..."
          cloudsmith whoami || {
            echo "Authentication failed!"
            exit 1
          }
          
          echo "Listing repositories..."
          cloudsmith list repos ${CLOUDSMITH_NAMESPACE} || {
            echo "Cannot access namespace ${CLOUDSMITH_NAMESPACE}"
            exit 1
          }
          
          echo "Checking repository details..."
          cloudsmith list packages ${CLOUDSMITH_NAMESPACE}/${CLOUDSMITH_REPOSITORY} --page-size 1 || {
            echo "Warning: Cannot list packages in ${CLOUDSMITH_NAMESPACE}/${CLOUDSMITH_REPOSITORY}"
            echo "This might indicate insufficient permissions"
          }
        shell: bash

      - name: Push package(s) to Cloudsmith
        run: |
          shopt -s nullglob
          
          FILES=(dist/*)
          if [ ${#FILES[@]} -eq 0 ]; then
            echo "No files found in dist/ to upload"
            exit 1
          fi

          echo "Uploading to: ${CLOUDSMITH_NAMESPACE}/${CLOUDSMITH_REPOSITORY}"
          
          for f in "${FILES[@]}"; do
            echo "Pushing $f..."
            cloudsmith push python "${CLOUDSMITH_NAMESPACE}/${CLOUDSMITH_REPOSITORY}" "$f" --republish --verbose || {
              echo "Failed to push $f"
              echo "Trying to get more details..."
              cloudsmith list repos ${CLOUDSMITH_NAMESPACE}
              exit 1
            }
          done
        shell: bash
